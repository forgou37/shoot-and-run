# CLAUDE.md

Living document for this project: architecture, hard rules, conventions, commands, and the Decisions Log. Keep it current — when a non-trivial choice is made, log it. When a command or convention changes, update it here in the same commit.

## What this is

A TowerFall Ascension–**inspired** local-multiplayer arena game. An homage, not a clone: all names, art, and content must be original.

- 2–4 players on one screen, archer-vs-archer combat
- Each player starts with 3 arrows; fired arrows stick in the world and must be picked up; out of arrows → kill by stomping enemies from above
- Screen wraps at all edges (horizontal and vertical)
- One-hit kills, rounds last 10–60 seconds, best-of-N match structure
- Tight platforming: variable jump height, coyote time, jump buffering, air control
- Small single-screen arenas, pixel art later, juice (hitstop/shake/particles) later
- Local multiplayer always works offline on one screen. **Online multiplayer added 2026-06-14** (owner reversal of the original "permanently out of scope" clause) — host-authoritative rollback netcode, dedicated-server-first, Cloudflare-native; see specs/008-netcode.md. The sim-purity, determinism, and tuning-is-data hard rules are unchanged and constrain the netcode design.

Long-term direction (specs 004–005, **not now**): an offline AI pipeline generates arena layouts as JSON, a headless simulator plays them with scripted bots, balance metrics + LLM-as-judge score them, winners land in `content/arenas/`. The architecture below exists so this is possible without a rewrite.

## Architecture

Monorepo (npm workspaces) with one hard boundary:

```
packages/sim    pure TypeScript game simulation — ZERO Phaser/DOM/canvas imports
packages/game   Phaser 3 shell — rendering, input devices, (later) audio/menus
content/        all game data as JSON — arenas, tuning, player configs
```

`packages/sim` owns **all** game rules: movement, physics, arrows, kills, round state. It is deterministic and runs headless in Node. `packages/game` is a thin shell: it translates keyboard/gamepad into plain input structs, calls `sim.step()` on a fixed timestep, and renders the resulting state with interpolation. No game logic in render callbacks, ever.

### Sim API contract (sketch — see specs/000-baseline.md)

```ts
const sim = createSim({ arena, tuning, players, seed });
const events = sim.step(inputs);   // advances exactly one 60 Hz tick, returns SimEvent[]
sim.state;                         // readonly snapshot for rendering / stats
```

The sim communicates outward **only** via returned events and readable state — no callbacks, no globals. This is the API the future AI pipeline consumes (init with arena + seed, step, read events/stats).

### Timing model

60 Hz fixed-timestep simulation driven by an accumulator in the shell. The shell may render at any refresh rate and interpolates entity positions between the previous and current sim tick. The sim itself has no concept of wall-clock time — only ticks.

## Hard rules

1. **Spec discipline.** Implement only what the current spec says. New ideas go to `specs/backlog.md`, not into the code.
2. **Sim purity.** `packages/sim` never imports Phaser, DOM, or canvas APIs — no `window`, `document`, `performance`, `requestAnimationFrame`. Enforced by a dependency-cruiser check (`npm run check:deps`) that runs in CI and must pass before merge.
3. **Tuning is data.** All game-feel tuning values (gravity, jump velocity, coyote time ms, jump buffer ms, arrow speed, …) live in ONE file: `content/tuning.json`. Hot-reloadable in dev. A hardcoded tunable number in source is a bug by definition.
4. **Deterministic sim.** Seeded RNG only inside `packages/sim`. No `Date.now()`, `Math.random()`, `performance.now()`, or any other ambient nondeterminism. Same arena + tuning + seed + input sequence ⇒ identical event log, always.
5. **One task per commit**, message prefixed with the task id (e.g. `T0.4: player movement with coyote time and jump buffer`).
6. **Placeholder visuals** (colored rects) until specs 000–002 are done. No art tasks before that. Pixel art is done by the owner, later.

## Conventions

- TypeScript strict mode everywhere. No `any` in `packages/sim`.
- Units: pixels. Tile size 16 px. Arena logical size 320×240 (20×15 tiles). Positions are floats in pixel units; the renderer may scale up integer-multiple.
- Durations in `content/tuning.json` are in milliseconds (designer-friendly); the sim converts to ticks at init (60 Hz ⇒ 1 tick ≈ 16.67 ms).
- Sim entity collections use plain arrays with stable insertion order; entity ids are assigned by a deterministic counter, never randomly.
- All content files are validated on load (schema validation lives in `packages/sim` so the headless pipeline gets it for free).
- Original naming only — no TowerFall terms, names, or assets anywhere in code or content.
- Tests: Vitest. Sim tests run headless in Node with no Phaser installed in their dependency tree.

## Content-as-data

Everything a designer (or a future LLM generator) might touch is a data file, never code:

| File | Contents |
|---|---|
| `content/arenas/*.json` | One arena per file: tile grid, spawn points, metadata. Conforms to the arena schema in `packages/sim`. |
| `content/tuning.json` | Every game-feel number. The only place tunables exist. Shell-only blocks `juice`, `input` (stickDeadzone), `ui` (lobbyCountdownMs), and `net` live here too — the sim ignores them all. The `net` block (validated in `packages/net`): `inputDelayTicks`/`snapshotIntervalTicks`/`maxRollbackTicks`/`jitterBufferTicks` (spec 008/009); `maxSpectators` (T13.2); `reconnectGraceTicks`/`reconnectAttempts`/`reconnectBackoffTicks` (T13.3); `maxInputsPerSecond`/`maxInputLeadTicks` (T13.5 hardening); `adaptiveInputDelay`/`minInputDelayTicks`/`maxInputDelayTicks`/`correctionSmoothingMs` (T13.6 lag-comp). |
| `content/players.json` | `{ slots: [{slot,name,color} ×4], keyboards: [KeyBindings ×2] }` — slot identities plus the two keyboard binding profiles. Devices bind to slots in the lobby, not here. |
| `content/bots.json` | `{ difficulties: { easy/normal/hard: {reactionDelayTicks, aimTolerance, aimErrorChance, dodgeChance, dashChance} } }` — bot difficulty presets (spec 004). Validated in `packages/bots`; never imported by the sim. |

Hot-reload: the Phaser shell watches `content/tuning.json` via Vite HMR in dev and pushes the new tuning object into the running sim. Note: hot-reloading mid-round breaks determinism for that run — fine in dev; replays/tests always pin the tuning snapshot at init.

## Project structure

```
arcade-game/
├─ CLAUDE.md
├─ package.json               # npm workspaces root: packages/*
├─ tsconfig.base.json
├─ .dependency-cruiser.cjs    # enforces sim + bots + net purity (hard rule 2); analyzes type-only edges
├─ specs/
│  ├─ 000-baseline.md
│  └─ backlog.md
├─ assets/                    # Aseprite sprite sources (art pass, spec 006); jungle-tiles/jungle-bg (spec 007) + castle-tiles/castle-bg (spec 016, dark-stone-castle theme); cards/ (cards.aseprite+cards.png — combined lobby character-card sheet, kept out of the export:art glob); fonts/ (FreePixel.ttf, the shell's pixel font)
├─ scripts/
│  ├─ export-art.mjs          # assets/*.aseprite → packages/game/public/assets/ (sprite atlases)
│  └─ slice-cards.mjs         # assets/cards/cards.png → public/assets/card_<name>.png (per-slot master cards)

├─ content/
│  ├─ arenas/arena-001.json   # "crossfire" — sim-test + golden-log fixture
│  ├─ arenas/arena-002.json   # "canopy" — jungle arena (online/server default; theme defaults to jungle when absent)
│  ├─ arenas/arena-003.json   # "ramparts" — castle arena the local shell boots into (spec 016; cosmetic theme:"castle")
│  ├─ tuning.json
│  ├─ players.json
│  └─ bots.json               # bot difficulty presets (spec 004)
├─ packages/
│  ├─ sim/
│  │  ├─ src/
│  │  │  ├─ index.ts          # public API: createSim, types
│  │  │  ├─ rng.ts            # seeded PRNG (mulberry32)
│  │  │  ├─ arena.ts          # arena types + schema validation
│  │  │  ├─ physics.ts        # wrap-aware AABB vs tile grid
│  │  │  ├─ player.ts         # movement, jump, stomp, power-up timers
│  │  │  ├─ arrow.ts          # flight per kind, sticking, pickup
│  │  │  ├─ kills.ts          # arrow/stomp/bomb kills
│  │  │  ├─ chest.ts          # chest spawn/open (PRNG-driven)
│  │  │  ├─ round.ts          # round + match state machine
│  │  │  ├─ events.ts         # SimEvent definitions
│  │  │  ├─ snapshot.ts       # spec 008: SimSnapshot + DOM-free deepClone (snapshot/restore)
│  │  │  └─ wire.ts           # spec 008: 1-byte input + versioned input-frame (de)serialization
│  │  └─ test/
│  │     └─ determinism.test.ts
│  ├─ bots/                   # spec 004: heuristic AI archers (pure, headless, imports only sim)
│  │  └─ src/
│  │     ├─ types.ts          # BotPolicy/BotContext/BotDifficulty contract
│  │     ├─ sense.ts          # wrap-aware perception primitives
│  │     ├─ bot.ts            # behavior stack (dodge→engage→scavenge), makeBot, botSeed
│  │     └─ config.ts         # content/bots.json validator
│  ├─ net/                    # spec 008: host-authoritative rollback netcode (pure, headless, imports only sim). types: transport/protocol/session; impl: loopback (latency/jitter/loss), codec (NetMessage wire — hello/join/reject/input/auth/snapshot/ack/ping/pong/lobby), host (authoritative loop + per-slot late-drop), host-runtime (join handshake + per-slot occupancy/reconnect + spectators + hardening + metrics), clock (tick sync + rttTicks), rollback (predict+rollback), client (ClientSession: predict/clock/spectator/reconnect/metrics/adaptive-delay), params (tuning.net validator), version (content-version guard). Spec 013 (T13.1–6): join handshake, spectators, reconnection (token reclaim), net metrics, hardening posture, lag-comp data. Real WebSocket transport lives in the shell + packages/server; no Cloudflare
│  ├─ server/                 # spec 011: self-hosted dedicated host = sim + net + a Node `ws` transport. ws-transport-server (ws→TransportServer adapter, optional direct-TLS wss), start.ts (createHostRuntime + 60 Hz loop + periodic health log + crypto.randomUUID reconnect tokens), main.ts (CLI entry — `npm run dev:host`/`start:host`, env PORT/HOST/PLAYERS/SEED/ARENA/CONTENT_DIR/TLS_CERT+TLS_KEY + JOIN_TOKEN). Headless Node, never imports Phaser/game (server-purity cruiser rule)
│  └─ game/
│     ├─ index.html
│     ├─ vite.config.ts
│     ├─ public/assets/       # committed sprite atlases (PNG + Aseprite JSON), via export:art; card_*.png (master-res lobby cards, drawn by the hi-res DOM overlay), via export:cards
│     └─ src/
│        ├─ main.ts           # Phaser game: registers boot→title→lobby→arena(+online) scenes
│        ├─ loop.ts           # accumulator + interpolation driver
│        ├─ app-context.ts    # app-wide singletons (DeviceManager, keyboard) in the registry
│        ├─ match-config.ts   # roster (slot+device+team) the lobby hands to the match
│        ├─ theme.ts          # pixel bitmap font: FreePixel→1-bit RetroFont atlas (buildPixelFont) + addPixelText/loadFont()
│        ├─ scene-transition.ts # spec 015: full-viewport DOM fade-through-black overlay (transitionTo/fadeIn wrap every scene change; above canvas + DOM card/input layers)
│        ├─ test-api.ts       # dev-only window.__testApi (getPhase + match probes + online getNetProbe[+metrics]/getConfirmedHashAt/forceDisconnect)
│        ├─ scenes/           # BootScene (?online/?spectate/?token/?netdebug), TitleScene (LOCAL/ONLINE), LobbyScene, ArenaScene (match + pause), OnlineJoinScene (host URL + join-token fields), OnlineArenaScene (online match: spectate/reconnect/net-overlay/correction-smoothing)
│        ├─ net/              # spec 010: WebSocketTransport (browser Transport impl over a DOM WebSocket)
│        ├─ input/            # InputDevice (keyboard/gamepad/bot), hot-plug manager, edge reader, players.json/tuning parsers
│        └─ render/           # sprite renderers (archer, arrows); environment.ts — theme-aware env (THEMES table maps autotile roles→tag names + tileset/bg per theme: jungle (spec 007) / castle (spec 016); ArenaTheme + themeFromArena()); cards.ts (card image URL) + card-overlay.ts (hi-res DOM card layer over the canvas); rect debug via ?rects=1
└─ packages/pipeline/         # FUTURE (spec 005): evals + generator. Do not create yet. (bots → packages/bots spec 004; net → packages/net spec 008)
```

## Commands

Keep this section current as scripts change.

| Command | Purpose |
|---|---|
| `npm run dev` | Vite dev server for `packages/game` (tuning hot-reload from T0.5) |
| `npm run dev:host` | Dedicated netcode host (`packages/server`, spec 010/011), run via `tsx`. Serves browser clients over WebSocket. Env: `PORT` (8787), `HOST` (all interfaces), `PLAYERS` (2), `SEED` (1), `ARENA` (arena-002.json), `CONTENT_DIR` (repo /content), `TLS_CERT`+`TLS_KEY` (optional → direct `wss://`). Open two tabs at `?online=ws://localhost:8787` to play |
| `npm run start:host` | Same dedicated host via the package's `start` script (`npm run start -w @shoot-and-run/server`) — the documented production entry; see the deployment doc for running on a VPS behind TLS |
| `npm run build` | Type-check both packages + production Vite build |
| `npm run typecheck` | `tsc --noEmit` over sim src + tests, bots src + tests (no Node/DOM types — purity), net src, game, server src + tests (Node types), and e2e |
| `npm test` | All Vitest suites (sim + bots + net + server tests run headless in Node; incl. the server's ws-adapter TCP test) |
| `npm run e2e` | Playwright suite (Chromium/SwiftShader, dev server, `window.__testApi`): shell smoke + lobby flow + gamepad shim + bots (`?bots=N` / lobby add-bot) + online two-tab match (spec 010, second `webServer` = `dev:host`) |
| `npm run lint` | ESLint, incl. sim + bots determinism guards (no `Math.random`/`Date.now`/timers) |
| `npm run check:deps` | dependency-cruiser: fails if `packages/sim/src`, `packages/bots/src`, `packages/net/src`, or `packages/server/src` imports outside its allowed set (bots + net may import the sim; sim imports nothing; server may import net + sim + ws + Node, never Phaser/game). Analyzes type-only edges (`tsPreCompilationDeps`) so the sim↔net types seam is enforced |
| `npm run export:art` | Re-export all `assets/*.aseprite` → `packages/game/public/assets/` atlases (needs local Aseprite; exports are committed, CI never runs this) |
| `npm run export:cards` | Slice `assets/cards/cards.png` → per-slot `public/assets/card_<name>.png` master cards (pure Node, no Aseprite; outputs committed) |

Notes: `packages/sim` has no build step — its package `exports` points at `src/index.ts` and Vite/Vitest consume the TS source directly. Sim's tsconfig has no DOM lib, so `window`/`document` fail to typecheck there (first line of defense for hard rule 2).

## CI/CD

GitHub Actions quality gate (`gate` + `e2e` jobs): `npm ci` → typecheck (`tsc --noEmit`) → `lint` → `check:deps` → `npm test` → `vite build`, plus the Playwright `e2e` job. Runs on every PR and on push to `main`. Trunk-based on `main`, one task per commit (hard rule 5); changes land via PR with both checks green — **server-enforced** by the "Protect main" ruleset (required checks `gate` + `e2e` strict, PR required, squash-only, force-push + deletion blocked, admin bypass; enabled once the repo went public, see [CONTRIBUTING.md](CONTRIBUTING.md) § Enforcement). Staged additions: validation of all `content/**/*.json` (with T0.3), Playwright shell smoke (spec 001), sim step-time benchmark vs budget (before spec 003).

The dev process — PR flow, roles, Definition of Done, releases — lives in [CONTRIBUTING.md](CONTRIBUTING.md); continuous deploy to GitHub Pages runs via `.github/workflows/deploy.yml` (push to `main`) and tag-triggered release builds via `.github/workflows/release.yml`.

- Linux CI re-verifying the golden determinism log produced on the Windows dev machine doubles as the cross-OS float-determinism check backing Decision #3.
- **CD: GitHub Pages continuous deploy is live** (`.github/workflows/deploy.yml`) — every green push to `main` builds `packages/game` and publishes to https://forgou37.github.io/shoot-and-run/, the always-playable playtest URL from spec 001. The Vite build uses a relative `base` so assets resolve under the project-pages subpath. Tagged releases to itch.io via `butler` remain later, once the game is fun.
- Generated arenas (spec 005) arrive as commits and pass the exact same gate as hand-written content.

## Decisions Log

Format: `Date | Scope | Decision | Reasoning | Alternatives rejected`

| Date | Scope | Decision | Reasoning | Alternatives rejected |
|---|---|---|---|---|
| 2026-06-12 | engine | Phaser 3 + TS + Vite | developer's primary stack, fastest iteration | Godot 4 rejected: stack consistency and web-first distribution preferred |
| 2026-06-12 | architecture | Engine-agnostic sim core in packages/sim, Phaser as rendering shell | enables headless simulation for future AI arena-generation pipeline and deterministic replay/testing | logic-inside-Phaser-scenes rejected: couples game rules to renderer, blocks headless mode |
| 2026-06-12 | sim-core | 60 Hz fixed-timestep accumulator with interpolated rendering; custom wrap-aware AABB-vs-tile-grid collision implemented in packages/sim; mulberry32 seeded PRNG | determinism + headless execution require zero engine dependencies; single-screen 20×15 tile arenas make tile-grid AABB simple and fast; edge wrapping needs custom collision handling anyway (an entity can overlap two opposite edges simultaneously, which engine broadphases don't model) | Phaser Arcade Physics rejected: imports Phaser → violates headless rule, and its variable-delta integration is not run-to-run deterministic; fixed-point math rejected: float determinism within a single JS engine is sufficient — no cross-machine lockstep is ever needed (online play permanently out of scope) |
| 2026-06-12 | tooling | npm workspaces + Vitest + ESLint + dependency-cruiser | zero extra toolchain for a 2-package solo repo; Vitest shares Vite's transform pipeline and runs sim tests in plain Node | pnpm/turborepo rejected: overkill at this scale; Jest rejected: slower TS/ESM story than Vitest alongside Vite |
| 2026-06-12 | testing | Layered e2e: game-rule e2e runs headless in packages/sim (bot-driven full rounds, spec 000 T0.9); browser e2e is a small Playwright shell-smoke suite via a dev-only `window.__testApi` hook, deferred to spec 001 | rules live entirely in sim, so browser re-testing would duplicate the suite in a slower, flakier place; keeps 000 lean | Playwright-in-000 rejected: shell glue is small enough to risk for one spec; rule-level browser e2e rejected: duplicates sim suite |
| 2026-06-12 | ci-cd | GitHub Actions single quality gate on every push (typecheck, lint, check:deps, Vitest, build from T0.1; content validation from T0.3; Playwright from 001; perf benchmark before 003); trunk-based on main with a green-gate rule; CD = static deploys only (Pages continuous from 001, itch.io via butler on tags later) | local-only multiplayer compiles to static files, so deployment is publishing the Vite build; Linux CI re-running the golden determinism log verifies cross-OS float determinism; generated arenas merge as commits and pass the same gate as hand-written content | PR-based flow rejected: solo repo, ceremony without reviewers — green-main rule gives the same safety; other CI hosts rejected: Actions is zero-setup alongside the GitHub remote; containerized/server deploys rejected: no server component exists or ever will |
| 2026-06-12 | ci-cd | Continuous Pages deploy deferred (amends the row above) | repo is private on a free plan — GitHub Pages unavailable; Cloudflare Pages needs an account decision by the owner | deploying anyway rejected: would require making the repo public as a side effect of a tooling choice |
| 2026-06-12 | sim-events | player_killed carries the victim's position (x, y) | shell FX (kill burst placement) need it now; pipeline balance metrics need kill heatmaps later; events are the sim's only output channel, so position must be in the payload | shell reading victim position from state rejected: at the time the event is consumed the round may already have reset; golden log consciously regenerated once for the payload change |
| 2026-06-12 | roadmap | Spec 002 re-scoped to treasure chests + special arrows (bomb/laser/bounce) + power-ups (invisibility/flight); gamepads/roster/menus deferred to 003; bots → 004, AI pipeline → 005 | owner direction: content/combat variety before input breadth; chests are also the first consumer of the seeded PRNG, proving the determinism design under randomness | keeping 002 as roster rejected by owner |
| 2026-06-12 | art | Pixel art is generated in-session by Claude driving Aseprite (MCP), owner as art director; sources in `assets/*.aseprite`, committed exports (PNG + Aseprite-JSON atlas) in `packages/game/public/assets/` consumed by the shell; per-slot colors via runtime ramp recolor of one canonical 16×16 sheet (spec 006, proposed) | owner delegated generation after reviewing the archer (19 frames, 6 tags); committed exports keep CI/CD free of an Aseprite dependency; one sheet + recolor avoids 4× asset upkeep | owner hand-drawing deferred: iteration speed; per-slot pre-exported sheets rejected; sprite sizes other than 16×16 rejected — matches tile grid, overlays the 12×12 hitbox |
| 2026-06-13 | art/content | Spec 007 (owner-directed, skipped pre-spec review): shell boots the new jungle arena-002 "canopy"; arena-001 stays the sim-test/golden fixture; tile variants chosen at render time by a wrap-aware exposure mask (8 frames + 2 vines, vines placed by a deterministic position hash); arrows rotate by `atan2(vy, vx)`, stuck arrows hold the last flight angle tracked shell-side | complete level art + a denser layout without touching the sim (golden log pinned to arena-001 stays byte-identical); autotiling keeps arenas pure collision data and the tileset tiny | per-tile art indices in arena JSON rejected: arenas remain collision data the generator pipeline can emit; storing stick angles in sim state rejected: cosmetic-only concern, sim purity wins |
| 2026-06-13 | sim-modes | Teams mode is implied by every player carrying `team?: 0\|1`; `friendlyFire?: boolean` defaults true; FFA paths kept byte-identical (golden log untouched) by branching round-end logic and gating kills behind a `team!==null` check; in teams mode `round_ended`/`match_ended.winner` carry the team id, match victory reads `match.teamScores` while per-player `scores` still tallies survivals | one sim, two modes without forking the kill/round code; the null-team guard guarantees FFA can never be suppressed so the determinism proof stays valid | a separate teams sim rejected: duplicates rules; regenerating the golden log rejected: FFA behavior is unchanged so it must stay identical |
| 2026-06-13 | shell-input/scenes | Spec 003: uniform `InputDevice {id,kind,connected,sample(),pausePressed()}` (keyboard profiles + standard-mapping pads, hot-plug via a DeviceManager); one app-wide DeviceManager+KeyboardInput in the Phaser registry; scene flow boot→title→lobby→arena with `?quickstart=1` skipping to the 2-keyboard match; pause is a shell-only flag that stops the accumulator (sim untouched); lobby assembles a MatchConfig roster the match consumes | keeps the sim seeing only PlayerInput; the device abstraction lets the lobby map devices→slots and the match auto-pause on pad-disconnect; freezing the accumulator (not the sim) preserves determinism | Phaser keyboard/gamepad plugins rejected: thin testable layer preferred; pausing via a separate sim concept rejected: pause is cosmetic, belongs in the shell |
| 2026-06-13 | sim-movement | Owner-directed movement adjustments: (1) wall-slide — while airborne, falling, and pressing into an adjacent wall, descent is capped at `wallSlideSpeed`; (2) straight-down edge fall — walking (not jumping) off a ledge zeroes carried horizontal velocity so you drop straight rather than arc (air control still steers); (3) dash — a short fast horizontal burst (`dashSpeed`/`dashDurationMs`/`dashCooldownMs`), edge-triggered, ground or air, suspends gravity for the burst, cancelled by a jump. New `PlayerInput.dash` bound to Left/Right Shift (keyboards, `content/players.json`) + gamepad button 5 (RB); all four numbers live in `content/tuning.json`. Golden FFA log stayed byte-identical (the scripted bot round on arena-001 ends on an early grounded arrow kill, so none of the new airborne/ledge mechanics fire) | requested couch-feel: wall cling for verticality, predictable drops off platforms, a mobility/dodge option; all rules stay in the pure sim so determinism + the future bot/eval pipeline are unaffected | automatic wall-slide rejected by owner (chose hold-into-wall for control); storing dash/wall state shell-side rejected: it's gameplay, belongs in the sim |
| 2026-06-13 | e2e | T3.5 forces software WebGL (`--use-gl=angle --use-angle=swiftshader`) for Playwright; cross-scene `__testApi.getPhase()` installed at boot, match-only probes augmented by ArenaScene; gamepad e2e drives a player via an injected `navigator.getGamepads` shim | headless Chromium's GPU WebGL context drops and only lazily restores, stalling Phaser's first boot into the loader-less title scene; SwiftShader is deterministic across local Windows + Linux CI | bumping the boot timeout rejected: masks the stall without fixing it; Playwright gamepad emulation rejected: none exists, a shim is the standard approach |
| 2026-06-14 | shell-text | All shell text renders via a runtime-generated 1-bit bitmap font (Phaser BitmapText + fixed-grid RetroFont) instead of live TTF canvas Text; FreePixel is rasterized once at boot into a thresholded glyph atlas (`buildPixelFont` in theme.ts), sampled NEAREST at integer scale, tinted per use, with 1px letter-spacing | a TTF drawn through the canvas 2D text API is always grayscale-anti-aliased, and those soft edges blur badly when the 320×240 buffer is nearest-upscaled ×N (the long-standing "blurry text" issue); a 1-bit atlas sampled NEAREST stays pixel-crisp at any integer scale in every browser | keeping TTF `Text` rejected: AA blur is unfixable under the upscale regardless of font choice; committing a pre-baked atlas PNG rejected: runtime generation needs no binary asset or headless-canvas dep and regenerates from the TTF |
| 2026-06-14 | netcode | Online multiplayer added (reverses "online never"). Host-authoritative model with deterministic client-side prediction + rollback re-sim; symmetric Host role (dedicated process **or** player browser, star topology, same `packages/sim` + a new `packages/net`); dedicated-server-first; Cloudflare-native infra (Pages client + Worker/Durable-Object control & host plane + Cloudflare TURN). New `packages/net` (imports sim, never Phaser/DOM) and `packages/server`; tuning pinned per net session. Phased as specs 008–013, foundation (snapshot/restore + input serialization + determinism hardening) is headless-only and gates the rest. | the deterministic, input-driven, fixed-timestep sim makes rollback nearly free and exact (right for one-hit kills); host authority absorbs float divergence so the fixed-point rejection stands; the sim has no transcendentals (only IEEE-correctly-rounded `Math.sqrt`) so it is near-certainly cross-machine deterministic — proven headlessly in 008 before any transport exists; dedicated-first defers NAT/TURN/host-migration; Cloudflare matches the existing Pages lean and runs the pure sim in `workerd` | snapshot-interpolation without prediction rejected: mushy, adds input latency; P2P deterministic lockstep rejected: doesn't fit dedicated hosting, stalls on the slowest peer, needs trustless cross-engine determinism; fixed-point still rejected: host authority makes it unnecessary; "no server component ever / static-only CD" amended: internet play needs signaling + a host process (kept serverless via Workers/DO, no always-on VM) |
| 2026-06-14 | sim-movement | Owner-directed wall jump: while airborne and clinging to a wall (pressing into an adjacent wall, the wall-slide state), a jump launches off it at exactly 45° — equal away-from-wall and upward components, both `wallJumpSpeed` — and turns to face away. Reuses `PlayerInput.jump` (no new binding); ranks below a ground/coyote jump and above a mid-air flap. Air control is suspended for `wallJumpControlLockMs` after the launch (new `wallJumpLockTicksLeft` state, cleared on landing) so the existing air-control clamp doesn't snap the horizontal launch speed back to `runSpeed` within a tick and flatten the arc; the lock leaves the parabola (gravity still applies). Both numbers in `content/tuning.json`; golden FFA log stayed byte-identical (the arena-001 bot round ends on an early grounded kill, so no airborne mechanic fires) | requested wall mobility — climb/reposition off walls; 45° because owner specified it; the control lock is required for the angle to actually read on screen, otherwise it collapses to ~vertical+runSpeed; all rules stay in the pure sim so determinism + the bot/eval pipeline are unaffected | a single up-only wall kick rejected: owner asked for 45°; storing the launch lock shell-side rejected: it's gameplay, belongs in the sim; reusing `wallSlideSpeed`/`jumpVelocity` for the launch rejected: the launch is its own feel knob |
| 2026-06-14 | bots | Spec 004: real bots live in a new pure `packages/bots` and are presented to the match as an `InputDevice` (`BotDevice`, `kind:"bot"`) whose `sample()` runs a heuristic `(state, slot, ctx) → PlayerInput` policy over the live `sim.state`; the sim, match scene, kills, rounds and teams are untouched. Each bot owns a mulberry32 seeded `botSeed(matchSeed, slot)` (never the sim's PRNG); behavior is a fixed priority stack (dodge→engage→scavenge) with all knobs in `content/bots.json` (easy/normal/hard). A `bots-purity` dependency-cruiser rule keeps `packages/bots/src` to sim-only imports. Headless eval `run-rounds` CLI deferred to spec 005 | bots are pure consumers of state and producers of input, so they belong outside the sim on the existing device seam — no rules change, determinism preserved, and the headless policy is exactly what the spec 005 eval pipeline will call; per-bot seeded RNG keeps bot-driven shell matches replayable | bots inside the sim rejected: they read state but aren't game rules, and would couple AI to the deterministic core; `Math.random` for aim error rejected: breaks replay — seeded PRNG instead; creating `packages/pipeline` now rejected: only bots are needed for couch play, evals/generator stay future |
| 2026-06-14 | shell-ui/content | Lobby upgraded in place from a text list to a four-card character-select screen (owner-directed): each card shows the per-slot recolored archer idle sprite scaled ×3 as the portrait, the slot name, and a status chip — a human (`joined`/`READY` + `KEY 1`/`PAD 2`) or a `BOT` + difficulty (purple), `+T1/T2` in teams. Slots renamed to real names (Lyosha, Igor B, Igor Sh, Maks) in `content/players.json`. The spec-003/004 lobby state machine (join/ready/team/mode/countdown + bot add/remove/difficulty) is unchanged — only `create()`/`render()` were rewritten into cards (persistent display objects mutated per frame; manual `bt.width` centering since the font is BitmapText). The archer recolor (`recolorArcherTexture`) + idle-frame lookup were extracted from `ArcherRenderer` into shared exports in `render/archer.ts` and reused by the lobby; the lobby `preload()` loads the archer atlas (guarded). Pure shell change: sim, determinism, golden log untouched | owner wanted a richer entrance screen like the reference; reusing the existing recolorable sprite avoids any new art this pass (real illustrated portraits remain a future art-pass); extracting the recolor avoids duplicating the ramp math; keeping the state machine intact preserves the bots feature and the `lobby.spec.ts`/`bots.spec.ts` e2e (driven by input + `getPhase`, not text) | a separate character-select scene rejected: the lobby already owns devices→slots and bot management; authoring new portrait art now rejected (deferred art pass); character/variant selection + arena select left in backlog |
| 2026-06-15 | shell-ui/content | Lobby card portraits replaced with owner-supplied illustrated character cards: one static 96×180 PNG per slot (`assets/card_*.aseprite` sources → committed `packages/game/public/assets/card_<name>.png`), each baking in its frame, full-body portrait and name banner. New `render/cards.ts` loads them keyed by normalized slot name (Lyosha→`card_lyosha`…, so the map follows `players.json` identity, not slot order); the lobby draws each at 0.75 scale (72×135) to fit four-across the 320 buffer (uniform 4→3 nearest downscale, cleanest for pixel art), dims empty cards to α0.35, and frames ready (green) / bot (purple) cards with a 2px outline drawn fully outside the art. The baked name banner replaces the separate name BitmapText; status + device/BOT/difficulty chips stack beneath each card (controller marked with `*` on its status). The lobby no longer uses the archer recolor (still used in-match); the spec-003/004 state machine, sim, determinism and golden log are all untouched. Verified: typecheck/lint/check:deps green, `lobby.spec.ts`/`bots.spec.ts` pass (zero console errors), all four PNGs serve 200 and decode to 96×180 art | owner delivered finished per-character card art, so rendering it directly is more faithful than the placeholder recolored-archer portraits and needs no atlas/recolor; name-normalized keys keep the mapping tied to roster identity; 0.75 scale is the largest clean fit since 4×96=384 overflows 320 | keeping recolored-archer portraits rejected: dedicated art now exists; native 96px display rejected: overflows the buffer; hardcoded slot-index→file map rejected: name-normalized keys track `players.json`; routing the static cards through `export:art`'s spritesheet/JSON pipeline rejected: they're single images loaded via `load.image` |
| 2026-06-15 | process | Adopt a production-like PR-based flow (reverses the 2026-06-12 "PR-based flow rejected" row). Every substantive change lands via a PR off `main`: Claude self-reviews (`/code-review` + `/security-review`) and gets the local gate green, then opens a PR with evidence; the owner reviews the diff and squash-merges. CI now triggers on `pull_request` + push to `main` (was every push). Branch protection first 403'd on the private Free plan, so the **repo was made public** (owner-approved) — unlocking a free "Protect main" ruleset, now **active**: required checks `gate` + `e2e` strict, PR required (0 approvals so the solo maintainer self-merges), squash-only merges, force-push + deletion blocked, repo-admin bypass. Going public also lifts the old "Pages unavailable on private Free" blocker on continuous deploy (CD still unwired pending a host pick). Added `CONTRIBUTING.md` (loop, roles, Definition of Done), `.github` PR + issue templates, `CHANGELOG.md`, and a tag-triggered `release.yml` (builds + GitHub Release; deploy still unwired pending the hosting decision). Pure process/infra — no sim/game/content change, golden log untouched | the original rejection ("solo repo, ceremony without reviewers") is obsolete now that the AI is a real first-pass reviewer/QA; inserting a review gate + tracked issues + release discipline catches bugs before `main` and makes the flow self-documenting; 0-approval protection fits a solo maintainer (GitHub forbids self-approval, so a ≥1 rule would deadlock) | requiring ≥1 approval rejected: deadlocks a solo dev; `enforce_admins` rejected: would lock the owner out of emergency direct pushes; a numbered `specs/` doc rejected: this is process, not a game feature — `CONTRIBUTING.md` is the canonical home and GitHub surfaces it on PR/issue creation; wiring deploy now rejected: host target still undecided (Pages public/paid, Cloudflare account call) |
| 2026-06-15 | ci-cd | Continuous deploy wired to **GitHub Pages** (resolves the 2026-06-12 "Pages deploy deferred/blocked" rows now that the repo is public). New `.github/workflows/deploy.yml` builds `packages/game` on every push to `main` and publishes `packages/game/dist` via the Actions Pages pipeline (`configure-pages` → `upload-pages-artifact` → `deploy-pages`, OIDC, no `gh-pages` branch) to https://forgou37.github.io/shoot-and-run/. The Vite build gets a **relative `base: './'`** (build only; dev/e2e stay `/`) so the bundle entry + the JS-imported FreePixel font resolve under the project-pages subpath; Phaser's runtime `load.*` paths were already relative. Deploy runs post-merge, so it only ships gate-green code | the deferred CD's sole blocker was the private-Free plan, lifted by going public; Actions-based Pages is the current first-party path (no extra push token or orphan branch); the conditional relative base keeps the Playwright dev-server suite on `/` untouched while fixing subpath asset URLs | `gh-pages`-branch deploy rejected: older, needs a push token + orphan branch; unconditional `base:'/shoot-and-run/'` rejected: breaks `npm run dev` + e2e at `/`; Cloudflare Pages deferred: GitHub Pages is zero-setup on the existing remote, Cloudflare stays the option for the netcode control plane (spec 010+) |
| 2026-06-15 | netcode | Spec 008 Phase 008 (netcode foundation, headless — T8.1–T8.6) implemented. (1) Hidden deterministic state surfaced: `Rng.getState/setState` + `Sim.getEntityIdCounter/setEntityIdCounter` (snapshot/restore only). (2) `sim.snapshot()` returns a deep, JSON-serializable `SimSnapshot` (SimState + RNG state + entity counter); `createSimFromSnapshot(snap, {arena,tuning,players,friendlyFire})` restores via a shared internal `buildSim` so step logic lives once and the determinism proof covers restore. (3) Clone is a hand-written DOM-free `deepClone` (sim tsconfig has no lib.dom → no `structuredClone`; JSON round-trip rejected for coercing -0/NaN/Infinity). (4) Input wire: 1 byte/`PlayerInput` (bit 0 left…6 dash, 7 reserved) + versioned frame `[varint PROTOCOL_VERSION][varint tick][u8 count][bytes]`, arithmetic LEB128, typed `ProtocolVersionError`/`WireFormatError`. (5) Cross-engine guard: checked-in FNV-1a `golden-state-hashes.json` over snapshots every 30 ticks of the bot round, re-verified by Linux CI. (6) `packages/net` skeleton — transport/protocol/session **types only**, imports sim, `net-purity` cruiser rule + `tsPreCompilationDeps:true` so the type-only sim↔net seam is actually enforced. Golden FFA event log byte-identical throughout (all additive). | the deterministic input-driven sim makes snapshot/restore + rollback re-sim exact and nearly free; funneling fresh + restored construction through one `buildSim` means the existing golden/determinism proofs validate the restore path for free; proving snapshot/RNG/counter capture headlessly retires all the determinism risk before any transport or browser code (009+) exists | `structuredClone` rejected (unavailable under the sim's no-DOM tsconfig) and JSON-clone rejected (lossy on signed-zero/non-finite); storing init-constants in the snapshot rejected — they are the session contract the caller supplies; per-tick version string rejected — a 1-byte numeric `PROTOCOL_VERSION` keeps frames compact; leaving `tsPreCompilationDeps` off rejected — the sim↔net seam is exchanged purely as types and would otherwise be invisible to `check:deps` |
| 2026-06-15 | netcode | Spec 008 Phase 009 (session layer, headless over loopback — T9.0–T9.6) implemented in `packages/net`. (T9.0) `LoopbackNetwork` implements the Transport/TransportServer seam in-process with seeded latency/jitter/loss, driven by an explicit `advance(tick)` (no wall-time) → fully reproducible. (T9.1) NetMessage codec `[varint PROTOCOL_VERSION][tag][payload]` for authoritative/snapshot/ack (input reuses the byte layout), reusing the sim's exported `encodeInputByte`/version/errors; local DOM-free varint + UTF-8 so `packages/sim` stays untouched. (T9.2) `HostSession`: per-(tick,slot) input buffer, repeat-last fill of missing inputs, canonical `step()`, broadcast authoritative + periodic snapshot + ack. (T9.3) `ClockSync`: estimates host tick from ack RTT (oneWay = RTT/2, EMA-smoothed) → ±0 under fixed delay. (T9.4) `RollbackController`: a confirmed sim fed only authoritative inputs (byte-identical to the host by determinism) + a predicted sim that guesses remote inputs (repeat-last) and rolls back/re-sims from the confirmed snapshot only on a real misprediction; `resync` heals loss gaps from periodic snapshots. (T9.5) end-to-end convergence harness: 2 scripted clients over the lossy loopback converge byte-for-byte to the host under {0%, 10% loss, heavy jitter}, reproducible per seed. (T9.6) net params (`inputDelayTicks`/`snapshotIntervalTicks`/`maxRollbackTicks`/`jitterBufferTicks`) live in a `net` block in `content/tuning.json`, validated by `parseNetParams`; the sim ignores it (golden artifacts untouched). Sim not touched this phase. | the confirmed/predicted split makes the convergence invariant trivial to assert (confirmed replays authoritative = host state) while prediction stays cosmetic; an in-process clock-driven loopback retires all the real-time risk (does rollback converge under loss/jitter?) deterministically in CI before any real transport (010) exists; net params as data follows hard rule 3 and the existing shell-only-block precedent | barrier waiting on the host rejected — repeat-last fill + client lead never stalls; embedding net params in source rejected (hard rule 3); a separate `content/net.json` rejected (owner chose the tuning.json block, O1); retransmission rejected for v1 — periodic snapshots heal loss gaps, simpler and bounded; modifying `packages/sim` to share varint/UTF-8 rejected — keeping it byte-untouched preserves the golden/determinism proofs by construction |
| 2026-06-15 | netcode | Post-merge `/code-review` + `/security-review` hardening of 008/009 (security review clean — the only untrusted surface is the codec; verified `JSON.parse`→`deepClone` does **not** pollute `Object.prototype`). Fixes: (1) `ClockSync` pairs each ack with its send by the echoed input tick (new `AckMessage.inputTick`) instead of a FIFO queue, so a dropped/reordered ack only loses its own sample — the old `shift()` permanently mis-paired after any loss. (2) The net codec now reuses the sim's `writeVarint`/`readVarint` (exported additively from `wire.ts`) instead of a private copy — **partially reverses** the 009 "keep sim byte-untouched / duplicate varint" call: an additive export changes no sim behavior (golden artifacts still byte-identical) yet removes the divergence risk. (3) `decodeMessage` validates a decoded snapshot's shape (`WireFormatError` on a malformed-but-parseable payload) before it reaches `resync`. (4) The host encodes each datagram once and broadcasts the bytes (`send` now takes `Uint8Array`) rather than re-encoding per client. (5) `RollbackController`: records the local input *before* the maxRollback cap (so a stall can't drop it), returns the `mispredicted` flag directly (dropping a twice-per-correction full-state `JSON.stringify`, and matching the spec's "iff a remote input differed" contract), and keeps the last-known remote inputs as the repeat-last guess across a `resync` instead of blanking them. (6) The convergence test now asserts clients fully recover to the host (`confirmedTick === hostTick` + byte-identical) under loss/jitter, not just past halfway; a clock loss test + a snapshot-validation test were added. (7) `jitterBufferTicks` documented as reserved (rollback already tolerates reordering; consumed in 010). Snapshot-wire JSON `-0`/`NaN`/`Infinity` caveat documented in the codec (safe today — the determinism hash already relies on JSON-stable state). Sim logic untouched; full gate green (197 tests). | the AI-as-first-pass-reviewer step (CONTRIBUTING.md) caught real latent bugs before they were built on in 010/011; each fix is localized and additive, keeping the determinism proofs intact; tick-paired acks + snapshot validation + encode-once are the right depth (mechanism, not a special case) | reworking the protocol to add a separate ping/seq channel rejected — echoing the existing input tick is enough to pair acks; a sentinel JSON codec for `-0`/`NaN`/`Infinity` rejected — the sim can't produce them and the determinism hash already assumes JSON-stable state, so a comment + the shape guard suffice; deferring these to backlog rejected — owner asked to fix all in this follow-up |
| 2026-06-15 | shell-ui/content | Lobby cards re-arted + rendered at full fidelity via a **hi-res DOM overlay** (owner-directed: "more quality in the cards"). New owner art arrives as one transparent sheet (`assets/cards/cards.aseprite`+`.png`); `scripts/slice-cards.mjs` (`npm run export:cards`, pure Node — auto-detects the four columns, common-Y crop + centre-pad, no downscale) writes per-slot master `card_<name>.png` (~156×557). Because the game renders into a fixed 320×240 buffer that is then nearest-upscaled, anything drawn through Phaser is capped at its tiny buffer footprint (cards were crushed to 42×150, ~8% of the art); so the cards are now drawn as plain DOM `<img>` over the canvas (`render/card-overlay.ts`), positioned from the canvas `getBoundingClientRect` and re-synced each frame via `render()` (the canvas is sized/centred a few frames after lobby create — laying out once was the alignment bug). `render/cards.ts` slimmed to a URL helper; the lobby drops the Phaser card Images + `preload`. Cards mapped sheet-L→R to identities (Igor Sh, Lyosha, Maks, Igor B) and `content/players.json` reordered so the lobby reads **Maks · Igor B · Lyosha · Igor Sh** (each player's colour moved with their name). Old per-card `assets/card_*.aseprite` removed (superseded). Verified: typecheck/lint/check:deps green, 142 unit tests, build, all e2e (shell+lobby+bots) pass; full-page screenshot confirms crisp aligned cards in every state. Sim/determinism/golden log untouched | the 320×240 pixel buffer is a hard cap on in-engine card detail and raising it globally would break the sim/content/golden-log or balloon scope for one screen; a lobby-only hi-res layer escapes the cap with no sim impact and is a common pixel-game pattern (crisp portraits in a pixel UI); per-frame re-layout is bulletproof against the canvas settling/resizing and is cheap for a static menu; auto-detecting columns keeps the slicer robust to re-drawn art | bigger in-buffer cards rejected: still blocky (~11%, also forces trimming the footer); doubling the global resolution rejected: breaks determinism/content or huge blast radius for one screen; Phaser DOM container (camera-scaled) rejected: reintroduces the buffer-size bottleneck via the intermediate logical-size box; routing cards through `export:art` still rejected (single images, not atlases) — hence the `assets/cards/` subdir outside its glob |
| 2026-06-15 | netcode | **Phase 010 re-scoped + implemented (T10.0–T10.6): real WebSocket transport + local two-tab play, NO Cloudflare** (owner pulled the cheapest tangible win out of the original "010 = transport + Cloudflare signaling"; signaling/room codes/`packages/server` → 011). The 009 host/clock/predict/rollback primitives now run over a real, non-loopback transport, wired into the live Phaser shell: **two browser tabs play a full match against a local dedicated Node host on `localhost`, converging byte-for-byte.** Pure additive net work — a `HelloMessage` handshake (codec); a transport-agnostic `ClientSession` (clock + predict/rollback over any `Transport`, driven by explicit `tick()`); a `HostRuntime` (connection→slot assignment + hello + "wait-for-all, start at tick 0", validating a dense in-range roster at init); plus `predict()`-returns-events and `predictedState()` on the rollback controller. Impure layers stay out of `packages/net`: a browser `WebSocketTransport` (DOM `WebSocket`) in `packages/game/src/net`, and the `ws`→`TransportServer` adapter + 60 Hz `setInterval` host in a dev-only `scripts/dev-host` run via `tsx` (`npm run dev:host`; `ws`/`tsx`/`@types/ws` added as root devDeps). Boot route `?online=ws://host:port`; online hot-reload disabled (tuning pinned, amendment #4); dev probes `getNetProbe`/`getConfirmedHashAt`. New tests: hello codec, ClientSession unit, HostRuntime+ClientSession convergence over the lossy loopback (now with inputs actually reaching the host — the proof the 009 test lacked), ws-adapter TCP echo, and a Playwright two-tab convergence e2e (`dev:host` as a second `webServer`). Post-landing `/code-review` + `/security-review`: core confirmed byte-identical-convergent + security-clean; two fixes applied (the HostRuntime roster guard above; the online scene snaps interpolation on a multi-tick prediction jump), three non-correctness findings deferred (pre-sync bootstrap input loss→011, rollback interpolation teleport→013, pre-open datagram drop benign — see specs/008-netcode.md § Phase 010 review notes). `packages/sim` untouched → golden FFA log + `golden-state-hashes.json` byte-identical. No new game-feel tunables (the `net` block already exists; PORT/PLAYERS/SEED are launch env, not tuning). Full gate green: typecheck/lint/check:deps + 211 unit tests + 11 e2e + build. | the deterministic 009 session made the only new risk "does clock-driven prediction converge over a real wall-clock transport?" — retired by the two-tab e2e; WebSocket forces a server endpoint (browsers can't peer over WS) so a local dedicated Node host is the faithful dedicated-first path minus Cloudflare; keeping every impure dep out of `packages/net` preserves the `net-purity` boundary and the determinism proofs; a dev script (not `packages/server`) honors the owner's "Cloudflare/server comes after" and the adapter graduates to the DO unchanged | `packages/server` now rejected (owner deferred Cloudflare; a `tsx` script suffices for localhost); `BroadcastChannel` (serverless same-origin two-tab) rejected — not a WebSocket and doesn't generalize to internet play; a browser as listen-server host rejected — that's 012 (P2P); reusing the host across e2e runs rejected — the monotonic connection counter makes it single-session (Playwright spawns a fresh host per run); local hitstop in the online scene rejected — freezing the client clock desyncs it from the host; TCP head-of-line caveat accepted for v1 (unreliable WebRTC DataChannel is 012, swappable behind the same `Transport` seam) |
| 2026-06-15 | netcode | **Phase 011 re-scoped + implemented (T11.1–T11.4): self-hosted dedicated server + online join menu — first internet match, NO Cloudflare** (owner: "pet project for me and friends, simplify it" — drops the original 011 Cloudflare Worker+DO signaling/room codes). The 010 host/clock/predict/rollback core is unchanged; 011 adds the layers around it for real internet play. (T11.1) `scripts/dev-host` graduates into a deployable `packages/server` workspace (sim + net + a Node `ws` transport): `ws-transport-server` (now with optional direct-`wss://` via `TLS_CERT`/`TLS_KEY`), `start.ts` (`startHost` + 60 Hz loop), `main.ts` (CLI, env `PORT`/`HOST`/`PLAYERS`/`SEED`/`ARENA`/`CONTENT_DIR`/`TLS_*`); `dev:host` repointed + new `start:host`; a `server-purity` cruiser rule (server → net+sim+ws+Node, never Phaser/game). (T11.2, pure `packages/net`) two real-RTT hardenings: a **clock bootstrap** (new `ping`/`pong` messages + `ClockSync.onPing/onPong` + oneWay tracking) so the client converges its clock *before* it leads — pre-fill while the host is parked at tick 0, then HOLD+ping until synced, then lead off the clock with a one-way-aware send floor — resolving the 010-deferred bootstrap input loss; and a **build/content version guard** (`computeContentVersion` FNV-1a over protocol+arena+tuning stamped into `HelloMessage`; mismatch → typed `VersionMismatchError` surfaced via `onError`) because the Pages client and VPS host are now deployed independently and must refuse loudly, not desync. (T11.3) a friend-usable shell flow: a Title LOCAL/ONLINE menu, an `OnlineJoinScene` (DOM host-URL field over the canvas, `localStorage`-remembered, `ws`/`wss` default by page scheme), a waiting lobby ("connected/expected" via a new broadcast `LobbyMessage`), and error recovery (version-mismatch / disconnect → message → back to the menu with the URL pre-filled); `?online=` deep-link still bypasses the menu. (T11.4) the two-tab e2e now joins tab A through the menu + tab B via deep-link, converging byte-for-byte; deployment doc (VPS + Caddy/nginx `wss` + direct-TLS + the https→wss mixed-content rule + recommended `inputDelayTicks`) in docs/05-netcode.md. The 010 loss-recovery test was made robust (assert recovery within a few snapshot intervals, not the exact final tick — under lossy no-retransmit a dropped tail snapshot is unrecoverable and every net-traffic change reshuffles the deterministic drop schedule; exact byte-identity stays proven by the clean-network test). `packages/sim` untouched → golden FFA log + `golden-state-hashes.json` byte-identical. No new game-feel tunables (server config is launch env; `net` block unchanged). Full gate green: typecheck/lint/check:deps (45 modules) + unit tests + online e2e + build. | the only genuinely new risk vs 010 was "does clock-driven prediction converge at real RTT, and what happens when client/host builds drift?" — retired by the clock-bootstrap + version guard, proven headless at 2/5/7-tick one-way delay; WebSocket from an https Pages client forces `wss://`, which a self-hosted Node process behind Caddy gives for free — no Cloudflare needed for a friends-only match; graduating the adapter into `packages/server` is exactly what 010 promised, and the `Transport` seam keeps a future managed host a drop-in | Cloudflare Worker/DO/TURN + room codes rejected for v1 (owner: pet-project scale; a self-hosted VPS process is enough — Workers/DO remain a future option behind the unchanged `Transport` seam); multiple concurrent rooms / matchmaking rejected (single game per host process); a `BroadcastChannel`/seed-picking fix for the loss test rejected (robust recovery assertion is honest about lossy no-retransmit); changing the `inputDelayTicks` default rejected — left at 3 (covers nearby friends) and documented for tuning, since it's pinned-per-session data; mid-session reconnection / team+character select over the wire / anti-cheat all deferred (013/backlog) |
| 2026-06-16 | netcode | **Spec 013 promoted to its own spec + implemented (T13.1–T13.6): netplay polish on the dedicated-server stack** (specs/013-netplay-polish.md). **Reordered ahead of 012**: the five non-P2P items ride the existing 011 host and need no player-hosting, so 013 is gated on 011 (not 012); **host migration split out to 012** (only meaningful with a player-host — a dedicated server has no peer to migrate to). (T13.1) a client→host `JoinMessage {role,version,reconnectToken?,joinToken?}` sent first so the host admits by intent + refuses a drifted build host-side; a tick-driven join-grace falls back to a legacy hello so a pre-013 client can't hang. (T13.2) spectators — `role:"spectator"` takes no slot/never gates the start/follows the broadcast (localSlot −1 → all-repeat-last predict), `maxSpectators` cap; HostSession broadcasts via an injected sink so the runtime fans out to spectators. (T13.3) reconnection — HostRuntime replaces its monotonic counter with per-slot occupancy (empty/connected/reserved/lost); a dropped slot is held `reconnectGraceTicks` and reclaimed via a host-issued per-slot token (`crypto.randomUUID`, injected so `net` stays randomness-free) + immediate snapshot; the scene auto-reconnects with bounded backoff. (T13.4) net metrics — `ClientSession.metrics()` (RTT/lead/rollbacks/resyncs, resyncs counts only real heals) + a `?netdebug=1`/F3 overlay + extended `getNetProbe()`; host per-slot late-drop + `HostRuntime.metrics()` + a periodic server health log. (T13.5) hardening posture (not behavioral anti-cheat) — 256 B datagram cap, per-conn input rate-limit, far-future input clamp, optional `JOIN_TOKEN` (new `token` reject reason). (T13.6) lag-comp as data — opt-in adaptive input delay (off by default; fixed `inputDelayTicks` ships) + shell-only correction smoothing (cosmetic). New `net`-block knobs only; server config (JOIN_TOKEN) is env. `packages/sim` untouched → golden FFA log + `golden-state-hashes.json` byte-identical throughout. Each task its own commit, full gate green per task: typecheck/lint/check:deps (47 modules) + 259 unit tests + 15 e2e (two-player + spectator + reconnect convergence, all byte-identical). | the deterministic host-authoritative core made each item localized + headless-provable; spectators/reconnect/anti-cheat are *cleaner* on a trusted dedicated server than on P2P, so doing them before 012 is the natural order; per-slot occupancy is the right model for both reconnect and a clean start-gate; injecting the token generator keeps the `net`-purity (no ambient randomness) intact; lag-comp as data follows hard rule 3 | **server-rewind lag compensation rejected** — incompatible with deterministic byte-for-byte convergence (it would make canonical state depend on per-client views); "lag-comp" redefined as input-delay strategy + correction smoothing (corrects the exploratory 008 posture); host migration kept in 012 (a dedicated host has no migration target); adaptive input delay shipped **off by default** (fixed delay is the proven convergence behavior); a dedicated reconnect-reason code rejected for v1 (reuse `reject:full` for expired/forged reclaims; only the join-token gate got its own `token` reason); behavioral anti-cheat (aimbot/wallhack) rejected — a friends-only deterministic host can't and needn't |
| 2026-06-16 | shell-ui/flow | **Spec 015: scene transitions** (T15.1–T15.2; owner-picked first item of the game-feel/flow polish set, the rest deferred to backlog). Every shell navigation now **fades through black** instead of hard-cutting: a single full-viewport DOM overlay (`packages/game/src/scene-transition.ts`, `z-index` 1000 — above the canvas **and** the lobby-card/online-input DOM layers) animated by CSS opacity. `transitionTo(scene,key,data)` (fade→`scene.start`, re-entrancy-guarded) replaces every outgoing `scene.start`; `fadeIn()` runs in each visible scene's `create()`; the overlay inits **opaque** so the first screen after boot (incl. `?quickstart`/`?online` deep-links) fades in from black. `BootScene` primes it + calls `setTransitionDurationMs` from tuning; its initial routes stay instant `scene.start` (boot is invisible, the target fades itself in). `LobbyScene` got a one-shot `started` guard so the fade delay can't re-fire `startMatch`. Duration is `ui.transitionMs` (220) in `content/tuning.json`, validated in `parseUiSettings` (hard rule 3). Pure shell — `packages/sim` untouched → golden FFA log + `golden-state-hashes.json` byte-identical. Browser-verified (opacity time-series: boot fade-in, title→lobby fade-out→start→fade-in; cards covered then rendered; no console errors); full gate green: typecheck/lint/check:deps (46 modules) + 230 unit tests + build + 15 e2e (incl. title→online-join→back + two-tab online convergence). | a Phaser `cameras.main.fade*` only covers the canvas and would leave the DOM card/input overlays fully visible mid-transition — a full-screen DOM overlay covers everything uniformly (the architecture deliberately composites canvas + DOM layers); fade-through-black is style-neutral and trivial over that mix; module-level duration config (set once at boot) avoids threading a value through every scene and works for AppContext-less scenes; the overlay must use explicit `top/left/width:100%/height:100%` (the `inset:0` shorthand collapsed a childless fixed div to 0×0 here — discovered in browser verification) | camera fade rejected (misses the DOM overlays); pixel-dissolve / iris-wipe styles rejected for v1 (fiddly to mask across DOM layers — deferred); input-gating / sim-freeze on the arena fade-in rejected (belongs to the deferred round-intro "ceremony" item, spec-016 candidate); threading the duration through every scene/`AppContext` rejected (module-global set at boot is simpler); committing a pre-baked transition asset rejected (a CSS-animated DOM div needs none) |
| 2026-06-16 | shell-art/content | **Spec 016 (owner-directed, from a downloaded AI reference sheet): second arena visual theme "Dark Stone Castle" alongside jungle.** New real-pixel-art sources authored in-session via the Aseprite MCP: `assets/castle-tiles.aseprite` — a 10-frame autotile set (`cap`/`cap-l`/`-r`/`-lr` lit-top stone, `block`+edges interior stone, `hang-a` chain + `hang-b` heraldic banner on transparency) — and `assets/castle-bg.aseprite` (320×240 moonlit castle: dithered night gradient, moon, crenellated tower silhouettes, warm lit windows); both exported to `packages/game/public/assets/castle-*` through the same Aseprite json-hash + `--list-tags` pipeline as the jungle atlas (so the committed atlas carries the `frameTags` the renderer maps by name). New `content/arenas/arena-003.json` "ramparts" — a symmetric battlement layout (4 spawns, 4 chest spots) with a cosmetic `theme:"castle"` field. `render/environment.ts` made **theme-aware**: a `THEMES` table maps abstract autotile *roles* (top-exposed surface / interior fill / two under-platform hangs) → each theme's frame-tag names + tileset/bg keys; new `ArenaTheme` + `themeFromArena(rawJson)` read the arena's cosmetic `theme` off the untyped import (the sim's `parseArena` strips unknown keys), defaulting to jungle. `ArenaScene` now boots arena-003 (castle); `OnlineArenaScene` + the dedicated server default stay jungle (arena-002). Pure shell/content/art — `packages/sim` untouched → golden FFA log + `golden-state-hashes.json` byte-identical. Verified: typecheck/lint/check:deps (46 modules) + 231 unit tests (incl. a new arena-003 content-validation case) + build + browser quickstart (the castle renders — autotiled battlements with hanging banners/chains over the moonlit bg, chests on the mid ledges, zero console errors, arena name "ramparts"). | the wrap-aware exposure-mask autotiler already separates *which role* a tile is from *which sprite* draws it, so parameterizing only the role→tag names + asset keys adds an entire themed level with **zero** sim impact and no second renderer; arena `theme` as a cosmetic JSON field keeps the level self-documenting for the future generator while the sim ignores it; the jungle default leaves arena-002 + the whole online path untouched | a separate castle renderer rejected (one role-driven autotiler covers both themes); adding `theme` to the sim's arena schema rejected (cosmetic-only → sim purity; the shell reads the raw import, parseArena ignores it); replacing the jungle theme rejected (owner chose keep-both, theme-switchable per arena); tracing the AI reference sheet pixel-for-pixel rejected (it is not real pixel art — authored clean 16×16 tiles in its style instead); note: the pixel MCP's `add_frame` copies the previous frame rather than creating an empty cel, so the two decoration frames were select-all + cut cleared before redrawing on transparency |
