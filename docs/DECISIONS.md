# Decisions Log

The architecture, tooling, and netcode decision history for this project — every non-trivial choice with its reasoning and the alternatives rejected. Split out of [CLAUDE.md](../CLAUDE.md) (2026-06-17) to keep the always-loaded project doc lean. The load-bearing invariants these decisions produced are codified in CLAUDE.md's **Hard rules** + **Conventions** and enforced by CI (`check:deps`, the determinism lint, `golden-state-hashes.json`).

**Append a row here whenever a non-trivial choice is made** (in the same PR).

Format: `Date | Scope | Decision | Reasoning | Alternatives rejected`

| Date | Scope | Decision | Reasoning | Alternatives rejected |
|---|---|---|---|---|
| 2026-06-12 | engine | Phaser 3 + TS + Vite | developer's primary stack, fastest iteration | Godot 4 rejected: stack consistency and web-first distribution preferred |
| 2026-06-12 | architecture | Engine-agnostic sim core in packages/sim, Phaser as rendering shell | enables headless simulation for future AI arena-generation pipeline and deterministic replay/testing | logic-inside-Phaser-scenes rejected: couples game rules to renderer, blocks headless mode |
| 2026-06-12 | sim-core | 60 Hz fixed-timestep accumulator with interpolated rendering; custom wrap-aware AABB-vs-tile-grid collision implemented in packages/sim; mulberry32 seeded PRNG | determinism + headless execution require zero engine dependencies; single-screen 20×15 tile arenas make tile-grid AABB simple and fast; edge wrapping needs custom collision handling anyway (an entity can overlap two opposite edges simultaneously, which engine broadphases don't model) | Phaser Arcade Physics rejected: imports Phaser → violates headless rule, and its variable-delta integration is not run-to-run deterministic; fixed-point math rejected: float determinism within a single JS engine is sufficient (host authority later absorbed cross-machine divergence — see 2026-06-14 netcode) |
| 2026-06-12 | tooling | npm workspaces + Vitest + ESLint + dependency-cruiser | zero extra toolchain for a 2-package solo repo; Vitest shares Vite's transform pipeline and runs sim tests in plain Node | pnpm/turborepo rejected: overkill at this scale; Jest rejected: slower TS/ESM story than Vitest alongside Vite |
| 2026-06-12 | testing | Layered e2e: game-rule e2e runs headless in packages/sim (bot-driven full rounds, spec 000 T0.9); browser e2e is a small Playwright shell-smoke suite via a dev-only `window.__testApi` hook, deferred to spec 001 | rules live entirely in sim, so browser re-testing would duplicate the suite in a slower, flakier place; keeps 000 lean | Playwright-in-000 rejected: shell glue is small enough to risk for one spec; rule-level browser e2e rejected: duplicates sim suite |
| 2026-06-12 | ci-cd | Original posture: GitHub Actions single quality gate on **every push**, trunk-based green-main rule, **static-only CD**, **no server component**, Pages deploy deferred (repo private) | local-only multiplayer compiled to static files; Linux CI re-running the golden determinism log verifies cross-OS float determinism | **All later reversed/resolved** — PR-based gate (2026-06-15 process), GitHub Pages CD (2026-06-15 ci-cd), dedicated server (2026-06-14 netcode) |
| 2026-06-12 | sim-events | player_killed carries the victim's position (x, y) | shell FX (kill burst placement) need it now; pipeline balance metrics need kill heatmaps later; events are the sim's only output channel, so position must be in the payload | shell reading victim position from state rejected: at the time the event is consumed the round may already have reset; golden log consciously regenerated once for the payload change |
| 2026-06-12 | roadmap | Spec 002 re-scoped to treasure chests + special arrows (bomb/laser/bounce) + power-ups (invisibility/flight); gamepads/roster/menus deferred to 003; bots → 004, AI pipeline → 005 | owner direction: content/combat variety before input breadth; chests are also the first consumer of the seeded PRNG, proving the determinism design under randomness | keeping 002 as roster rejected by owner |
| 2026-06-12 | art | Pixel art is generated in-session by Claude driving Aseprite (MCP), owner as art director; sources in `assets/*.aseprite`, committed exports (PNG + Aseprite-JSON atlas) in `packages/game/public/assets/` consumed by the shell; per-slot colors via runtime ramp recolor of one canonical 16×16 sheet (spec 006, proposed) | owner delegated generation after reviewing the archer (19 frames, 6 tags); committed exports keep CI/CD free of an Aseprite dependency; one sheet + recolor avoids 4× asset upkeep | owner hand-drawing deferred: iteration speed; per-slot pre-exported sheets rejected; sprite sizes other than 16×16 rejected — matches tile grid, overlays the 12×12 hitbox |
| 2026-06-13 | art/content | Spec 007 (owner-directed, skipped pre-spec review): shell boots the new jungle arena-002 "canopy"; arena-001 stays the sim-test/golden fixture; tile variants chosen at render time by a wrap-aware exposure mask (8 frames + 2 vines, vines placed by a deterministic position hash); arrows rotate by `atan2(vy, vx)`, stuck arrows hold the last flight angle tracked shell-side | complete level art + a denser layout without touching the sim (golden log pinned to arena-001 stays byte-identical); autotiling keeps arenas pure collision data and the tileset tiny | per-tile art indices in arena JSON rejected: arenas remain collision data the generator pipeline can emit; storing stick angles in sim state rejected: cosmetic-only concern, sim purity wins |
| 2026-06-13 | sim-modes | Teams mode is implied by every player carrying `team?: 0\|1`; `friendlyFire?: boolean` defaults true; FFA paths kept byte-identical (golden log untouched) by branching round-end logic and gating kills behind a `team!==null` check; in teams mode `round_ended`/`match_ended.winner` carry the team id, match victory reads `match.teamScores` while per-player `scores` still tallies survivals | one sim, two modes without forking the kill/round code; the null-team guard guarantees FFA can never be suppressed so the determinism proof stays valid | a separate teams sim rejected: duplicates rules; regenerating the golden log rejected: FFA behavior is unchanged so it must stay identical |
| 2026-06-13 | shell-input/scenes | Spec 003: uniform `InputDevice {id,kind,connected,sample(),pausePressed()}` (keyboard profiles + standard-mapping pads, hot-plug via a DeviceManager); one app-wide DeviceManager+KeyboardInput in the Phaser registry; scene flow boot→title→lobby→arena with `?quickstart=1` skipping to the 2-keyboard match; pause is a shell-only flag that stops the accumulator (sim untouched); lobby assembles a MatchConfig roster the match consumes | keeps the sim seeing only PlayerInput; the device abstraction lets the lobby map devices→slots and the match auto-pause on pad-disconnect; freezing the accumulator (not the sim) preserves determinism | Phaser keyboard/gamepad plugins rejected: thin testable layer preferred; pausing via a separate sim concept rejected: pause is cosmetic, belongs in the shell |
| 2026-06-13 | sim-movement | Owner-directed movement adjustments: (1) wall-slide — while airborne, falling, and pressing into an adjacent wall, descent is capped at `wallSlideSpeed`; (2) straight-down edge fall — walking (not jumping) off a ledge zeroes carried horizontal velocity so you drop straight rather than arc (air control still steers); (3) dash — a short fast horizontal burst (`dashSpeed`/`dashDurationMs`/`dashCooldownMs`), edge-triggered, ground or air, suspends gravity for the burst, cancelled by a jump. New `PlayerInput.dash` bound to Left/Right Shift (keyboards, `content/players.json`) + gamepad button 5 (RB); all four numbers live in `content/tuning.json`. Golden FFA log stayed byte-identical (the scripted bot round on arena-001 ends on an early grounded arrow kill, so none of the new airborne/ledge mechanics fire) | requested couch-feel: wall cling for verticality, predictable drops off platforms, a mobility/dodge option; all rules stay in the pure sim so determinism + the future bot/eval pipeline are unaffected | automatic wall-slide rejected by owner (chose hold-into-wall for control); storing dash/wall state shell-side rejected: it's gameplay, belongs in the sim |
| 2026-06-13 | e2e | T3.5 forces software WebGL (`--use-gl=angle --use-angle=swiftshader`) for Playwright; cross-scene `__testApi.getPhase()` installed at boot, match-only probes augmented by ArenaScene; gamepad e2e drives a player via an injected `navigator.getGamepads` shim | headless Chromium's GPU WebGL context drops and only lazily restores, stalling Phaser's first boot into the loader-less title scene; SwiftShader is deterministic across local Windows + Linux CI | bumping the boot timeout rejected: masks the stall without fixing it; Playwright gamepad emulation rejected: none exists, a shim is the standard approach |
| 2026-06-14 | shell-text | All shell text renders via a runtime-generated 1-bit bitmap font (Phaser BitmapText + fixed-grid RetroFont) instead of live TTF canvas Text; FreePixel is rasterized once at boot into a thresholded glyph atlas (`buildPixelFont` in theme.ts), sampled NEAREST at integer scale, tinted per use, with 1px letter-spacing | a TTF drawn through the canvas 2D text API is always grayscale-anti-aliased, and those soft edges blur badly when the 320×240 buffer is nearest-upscaled ×N (the long-standing "blurry text" issue); a 1-bit atlas sampled NEAREST stays pixel-crisp at any integer scale in every browser | keeping TTF `Text` rejected: AA blur is unfixable under the upscale regardless of font choice; committing a pre-baked atlas PNG rejected: runtime generation needs no binary asset or headless-canvas dep and regenerates from the TTF |
| 2026-06-14 | netcode | Online multiplayer added (reverses "online never"). Host-authoritative model with deterministic client-side prediction + rollback re-sim; symmetric Host role (dedicated process **or** player browser, star topology, same `packages/sim` + a new `packages/net`); dedicated-server-first; tuning pinned per net session. Phased as specs 008–013; the snapshot/restore + input-serialization foundation is headless-only and gates the rest. (Original plan was Cloudflare-native; 011 simplified to a self-hosted VPS — no Cloudflare.) | the deterministic, input-driven, fixed-timestep sim makes rollback nearly free and exact (right for one-hit kills); host authority absorbs float divergence so the fixed-point rejection stands; the sim has no transcendentals (only IEEE-correct `Math.sqrt`) so it is near-certainly cross-machine deterministic — proven headlessly in 008 before any transport exists | snapshot-interpolation without prediction rejected: mushy, adds input latency; P2P deterministic lockstep rejected: stalls on the slowest peer, needs trustless cross-engine determinism; fixed-point still rejected: host authority makes it unnecessary; "no server component ever" amended: internet play needs a host process |
| 2026-06-14 | sim-movement | Owner-directed wall jump: while airborne and clinging to a wall (pressing into an adjacent wall, the wall-slide state), a jump launches off it at exactly 45° — equal away-from-wall and upward components, both `wallJumpSpeed` — and turns to face away. Reuses `PlayerInput.jump` (no new binding); ranks below a ground/coyote jump and above a mid-air flap. Air control is suspended for `wallJumpControlLockMs` after the launch (new `wallJumpLockTicksLeft` state, cleared on landing) so the existing air-control clamp doesn't snap the horizontal launch speed back to `runSpeed` within a tick and flatten the arc; the lock leaves the parabola (gravity still applies). Both numbers in `content/tuning.json`; golden FFA log stayed byte-identical (the arena-001 bot round ends on an early grounded kill, so no airborne mechanic fires) | requested wall mobility — climb/reposition off walls; 45° because owner specified it; the control lock is required for the angle to actually read on screen, otherwise it collapses to ~vertical+runSpeed; all rules stay in the pure sim so determinism + the bot/eval pipeline are unaffected | a single up-only wall kick rejected: owner asked for 45°; storing the launch lock shell-side rejected: it's gameplay, belongs in the sim; reusing `wallSlideSpeed`/`jumpVelocity` for the launch rejected: the launch is its own feel knob |
| 2026-06-14 | bots | Spec 004: real bots live in a new pure `packages/bots` and are presented to the match as an `InputDevice` (`BotDevice`, `kind:"bot"`) whose `sample()` runs a heuristic `(state, slot, ctx) → PlayerInput` policy over the live `sim.state`; the sim, match scene, kills, rounds and teams are untouched. Each bot owns a mulberry32 seeded `botSeed(matchSeed, slot)` (never the sim's PRNG); behavior is a fixed priority stack (dodge→engage→scavenge) with all knobs in `content/bots.json` (easy/normal/hard). A `bots-purity` dependency-cruiser rule keeps `packages/bots/src` to sim-only imports. Headless eval `run-rounds` CLI deferred to spec 005 | bots are pure consumers of state and producers of input, so they belong outside the sim on the existing device seam — no rules change, determinism preserved, and the headless policy is exactly what the spec 005 eval pipeline will call; per-bot seeded RNG keeps bot-driven shell matches replayable | bots inside the sim rejected: they read state but aren't game rules, and would couple AI to the deterministic core; `Math.random` for aim error rejected: breaks replay — seeded PRNG instead; creating `packages/pipeline` now rejected: only bots are needed for couch play, evals/generator stay future |
| 2026-06-14→15 | shell-ui/content | Two earlier lobby-card iterations (recolored-archer portraits, then static 96×180 PNG cards) — **both superseded by the 2026-06-15 hi-res DOM card overlay row below**. Real player names (Maks/Igor B/Lyosha/Igor Sh) and the unchanged spec-003/004 lobby state machine date from here. | — | — |
| 2026-06-15 | process | Adopt a production-like PR-based flow (reverses the 2026-06-12 "PR-based flow rejected" row). Every substantive change lands via a PR off `main`: Claude self-reviews (`/code-review` + `/security-review`) and gets the local gate green, then opens a PR with evidence; the owner reviews and squash-merges. CI triggers on `pull_request` + push to `main`. The **repo was made public** (owner-approved) to unlock a free "Protect main" ruleset, now active: required checks `gate` + `e2e` strict, PR required (0 approvals so the solo maintainer self-merges), squash-only, force-push + deletion blocked, repo-admin bypass. Added `CONTRIBUTING.md`, `.github` templates, `CHANGELOG.md`, and a tag-triggered `release.yml`. | the original rejection ("solo repo, ceremony without reviewers") is obsolete now that the AI is a real first-pass reviewer/QA; a review gate + tracked issues catches bugs before `main`; 0-approval protection fits a solo maintainer (GitHub forbids self-approval, so ≥1 would deadlock) | requiring ≥1 approval rejected: deadlocks a solo dev; `enforce_admins` rejected: would lock the owner out of emergency pushes |
| 2026-06-15 | ci-cd | Continuous deploy wired to **GitHub Pages** (resolves the 2026-06-12 "Pages deploy deferred/blocked" rows now that the repo is public). New `.github/workflows/deploy.yml` builds `packages/game` on every push to `main` and publishes `packages/game/dist` via the Actions Pages pipeline (`configure-pages` → `upload-pages-artifact` → `deploy-pages`, OIDC, no `gh-pages` branch) to https://forgou37.github.io/shoot-and-run/. The Vite build gets a **relative `base: './'`** (build only; dev/e2e stay `/`) so the bundle entry + the JS-imported FreePixel font resolve under the project-pages subpath; Phaser's runtime `load.*` paths were already relative. Deploy runs post-merge, so it only ships gate-green code | the deferred CD's sole blocker was the private-Free plan, lifted by going public; Actions-based Pages is the current first-party path (no extra push token or orphan branch); the conditional relative base keeps the Playwright dev-server suite on `/` untouched while fixing subpath asset URLs | `gh-pages`-branch deploy rejected: older, needs a push token + orphan branch; unconditional `base:'/shoot-and-run/'` rejected: breaks `npm run dev` + e2e at `/`; Cloudflare Pages deferred: GitHub Pages is zero-setup on the existing remote, Cloudflare stays the option for the netcode control plane (spec 010+) |
| 2026-06-15 | netcode | Netcode foundation 008 + session layer 009 implemented headless in `packages/net` (full task manifest in specs/008-netcode.md). **008:** surfaced deterministic hidden state (RNG + entity-counter get/set), `sim.snapshot()`/`createSimFromSnapshot` via a shared `buildSim` (so the determinism proof covers restore), DOM-free `deepClone`, 1-byte input wire + versioned frame, checked-in FNV-1a `golden-state-hashes.json` re-verified by Linux CI, and a types-only `packages/net` skeleton (`net-purity` rule + `tsPreCompilationDeps`). **009:** `LoopbackNetwork` (seeded latency/jitter/loss, tick-driven), NetMessage codec, `HostSession` (per-slot buffer + repeat-last fill), `ClockSync` (ack-RTT→host-tick EMA), `RollbackController` (confirmed sim fed authoritative inputs + predicted sim that rolls back only on a real misprediction; `resync` heals loss gaps), an end-to-end convergence harness (clients converge byte-for-byte under loss/jitter), and the `net` tuning block. **Post-merge hardening** (`/code-review` + `/security-review`, codec is the only untrusted surface): tick-paired acks (loss no longer permanently mis-pairs), codec reuses the sim's varint, snapshot shape-validation in `decodeMessage`, encode-once broadcast, record-local-input-before-cap. Sim byte-untouched throughout. | the deterministic input-driven sim makes snapshot/restore + rollback exact and nearly free; one `buildSim` path means the existing golden/determinism proofs validate restore for free; a clock-driven loopback retires the loss/jitter convergence risk in CI before any real transport exists; net params as data follows hard rule 3 | `structuredClone` rejected (no DOM lib) and JSON-clone rejected (lossy on -0/NaN/Infinity); P2P lockstep / retransmission rejected — periodic snapshots heal loss gaps, bounded and simpler; embedding net params in source rejected (hard rule 3) |
| 2026-06-15 | shell-ui/content | Lobby character cards rendered at full fidelity via a **hi-res DOM overlay** (owner: "more quality in the cards"; replaces all earlier in-buffer card attempts). Owner art is one transparent sheet (`assets/cards/cards.*`); `scripts/slice-cards.mjs` (`npm run export:cards`, pure Node) writes per-slot master `card_<name>.png`. Because the game renders into a fixed 320×240 buffer that is nearest-upscaled, anything drawn through Phaser is capped at its tiny buffer footprint — so cards are drawn as plain DOM `<img>` over the canvas (`render/card-overlay.ts`), positioned from the canvas rect and re-synced each frame (the canvas settles a few frames after lobby create — laying out once was the alignment bug). `render/cards.ts` is now a URL helper. Cards map to identities and `content/players.json` is ordered **Maks · Igor B · Lyosha · Igor Sh**. | the 320×240 buffer is a hard cap on in-engine card detail; raising it globally would break the sim/golden-log or balloon scope; a lobby-only hi-res DOM layer escapes the cap with no sim impact (a common pixel-UI pattern); per-frame re-layout is robust to the canvas settling/resizing | bigger in-buffer cards rejected (still blocky); doubling global resolution rejected (breaks determinism/content, huge blast radius); Phaser DOM container rejected (reintroduces the buffer-size cap) |
| 2026-06-15 | netcode | **Phase 010: real WebSocket transport + local two-tab play, NO Cloudflare** (owner pulled the cheapest tangible win; signaling/room codes → 011). The 009 host/clock/predict/rollback primitives now run over a real transport wired into the live Phaser shell: two browser tabs play a full match against a local Node host on `localhost`, converging byte-for-byte. Additive net work — a `HelloMessage` handshake; a transport-agnostic `ClientSession` (clock + predict/rollback over any `Transport`, explicit `tick()`); a `HostRuntime` (connection→slot, hello, wait-for-all start at tick 0, dense-roster validation). Impure layers stay out of `packages/net`: a browser `WebSocketTransport` and a dev-only `ws`→`TransportServer` host script (`npm run dev:host`). Boot route `?online=ws://host:port`; online hot-reload disabled (tuning pinned). | the deterministic 009 session reduced the only new risk to "does clock-driven prediction converge over a real wall-clock transport?" — retired by the two-tab e2e; WebSocket forces a server endpoint (browsers can't peer over WS) so a local Node host is the faithful dedicated-first path minus Cloudflare; keeping impure deps out of `packages/net` preserves the determinism proofs | `BroadcastChannel` rejected (not a WebSocket, doesn't generalize to internet play); browser-as-listen-server rejected (that's P2P, 012); TCP head-of-line caveat accepted for v1 (unreliable WebRTC DataChannel is 012, swappable behind the same `Transport` seam) |
| 2026-06-15 | netcode | **Phase 011: self-hosted dedicated server + online join menu — first internet match, NO Cloudflare** (owner: "pet project for me and friends, simplify it"; drops the original Cloudflare Worker+DO signaling/room codes). The 010 core is unchanged; 011 adds the surrounding layers. (T11.1) `scripts/dev-host` graduates into a deployable `packages/server` (sim + net + Node `ws`, optional direct-`wss://` via `TLS_*`, CLI env `PORT`/`HOST`/`PLAYERS`/`SEED`/`ARENA`/`CONTENT_DIR`/`TLS_*`; `server-purity` cruiser rule). (T11.2, pure `packages/net`) two real-RTT hardenings: a **clock bootstrap** (`ping`/`pong` so the client syncs its clock *before* it leads — fixes the 010-deferred bootstrap input loss) and a **content-version guard** (FNV-1a over protocol+arena+tuning in `HelloMessage`; mismatch → `VersionMismatchError`) because client and host now deploy independently. (T11.3) a friend-usable shell flow: a Title LOCAL/ONLINE menu, an `OnlineJoinScene` (DOM host-URL field, `localStorage`-remembered), a waiting lobby (broadcast `LobbyMessage`), and disconnect/version error recovery back to the menu; `?online=` deep-link still bypasses it. Deployment doc (VPS + Caddy/nginx `wss` + https→wss mixed-content rule + recommended `inputDelayTicks`) in docs/05-netcode.md. | the new risk vs 010 was "does prediction converge at real RTT, and what if builds drift?" — retired by the clock-bootstrap + version guard, proven headless at 2/5/7-tick one-way delay; an https Pages client forces `wss://`, which a Node process behind Caddy gives for free — no Cloudflare needed for a friends-only match; the `Transport` seam keeps a future managed host a drop-in | Cloudflare Worker/DO/TURN + room codes rejected for v1 (a self-hosted VPS is enough); matchmaking / multiple rooms rejected (single game per host); `inputDelayTicks` default left at 3 (covers nearby friends); mid-session reconnection / select-over-the-wire / anti-cheat deferred (013/backlog) |
| 2026-06-16 | netcode | **Spec 013: netplay polish on the dedicated-server stack** (specs/013-netplay-polish.md). Reordered ahead of 012 (these items ride the existing 011 host, need no player-hosting); host migration split to 012 (a dedicated host has no peer to migrate to). (T13.1) a client→host `JoinMessage` sent first so the host admits by intent + refuses a drifted build, with a join-grace fallback for legacy clients. (T13.2) spectators — `role:"spectator"` takes no slot, never gates the start, follows the broadcast; `maxSpectators` cap. (T13.3) reconnection — per-slot occupancy (empty/connected/reserved/lost); a dropped slot is held `reconnectGraceTicks` and reclaimed via a host-issued token (`crypto.randomUUID`, injected so `net` stays randomness-free); the scene auto-reconnects with bounded backoff. (T13.4) net metrics — `ClientSession.metrics()` + `?netdebug=1`/F3 overlay + host health log. (T13.5) hardening posture — datagram cap, input rate-limit, far-future clamp, optional `JOIN_TOKEN`. (T13.6) lag-comp as data — opt-in adaptive input delay (off by default) + shell-only correction smoothing. New `net`-block knobs only. | the deterministic host-authoritative core made each item localized + headless-provable; spectators/reconnect/hardening are *cleaner* on a trusted dedicated server than on P2P, so before 012 is the natural order; injecting the token generator keeps `net`-purity | **server-rewind lag compensation rejected** — incompatible with deterministic byte-for-byte convergence (canonical state would depend on per-client views); "lag-comp" redefined as input-delay + correction smoothing; adaptive input delay shipped **off by default** (fixed delay is the proven behavior); behavioral anti-cheat rejected — a friends-only deterministic host can't and needn't |
| 2026-06-16 | shell-ui/flow | **Spec 015: scene transitions** — every shell navigation now **fades through black** via a single full-viewport DOM overlay (`scene-transition.ts`, `z-index` 1000, above the canvas **and** the DOM card/input layers), animated by CSS opacity. `transitionTo()` wraps every outgoing `scene.start`; `fadeIn()` runs in each scene's `create()`; the overlay inits opaque so the first screen fades in from black. Duration is `ui.transitionMs` in `content/tuning.json` (hard rule 3). | a Phaser camera fade only covers the canvas and would leave the DOM card/input overlays visible mid-transition — a full-screen DOM overlay covers the composited canvas + DOM layers uniformly; the overlay needs explicit `top/left/width/height` (the `inset:0` shorthand collapsed a childless fixed div to 0×0 here) | camera fade rejected (misses the DOM overlays); pixel-dissolve / iris-wipe deferred (fiddly across DOM layers); threading duration through every scene rejected (module-global set at boot) |
| 2026-06-16 | shell-art/content | **Spec 016: second arena visual theme "Dark Stone Castle" alongside jungle.** New pixel-art sources (`assets/castle-tiles.aseprite` — a 10-frame autotile set: lit-top + interior stone + chain/banner hangs — and `assets/castle-bg.aseprite`, a moonlit castle) exported through the same Aseprite pipeline as the jungle atlas (committed atlas carries the `frameTags` the renderer maps by name). New `content/arenas/arena-003.json` "ramparts" with a cosmetic `theme:"castle"` field. `render/environment.ts` made **theme-aware**: a `THEMES` table maps abstract autotile *roles* (top surface / interior fill / under-platform hangs) → each theme's frame-tag names + tileset/bg keys; `themeFromArena()` reads the arena's cosmetic `theme` off the untyped import (sim's `parseArena` strips it), defaulting to jungle. `ArenaScene` boots castle (arena-003); online + dedicated server stay jungle (arena-002). | the exposure-mask autotiler already separates *which role* a tile is from *which sprite* draws it, so parameterizing role→tag + asset keys adds a whole themed level with **zero** sim impact and no second renderer; arena `theme` as a cosmetic JSON field keeps the level self-documenting for the future generator while the sim ignores it | a separate castle renderer rejected (one role-driven autotiler covers both); adding `theme` to the sim's arena schema rejected (cosmetic-only → sim purity); replacing jungle rejected (owner chose keep-both); note: the pixel MCP's `add_frame` copies the previous frame, so decoration frames were cleared before redrawing on transparency |
| 2026-06-17 | shell-ui/flow | **Spec 017: lobby character select.** The "press a button to join" lobby (spec 003) auto-claimed the lowest free card; now a joined-but-not-ready human moves their selection across cards with left/right (arrows / d-pad / stick) and locks it with jump. Each card holds **at most one occupant** — navigation only ever lands on a card no other human or bot holds, so a chosen character is exclusive without a separate "lock" state. In teams mode a player's team is **derived from the chosen card's column parity** (even→T1, odd→T2), retiring the old left/right team-switch. The host's bot add/remove moved off left/right onto a small modal: **dash** opens a placement cursor on the first empty card, left/right pick a non-human card, jump adds (empty) or removes (bot) a bot, shoot/dash cancels; the countdown is suspended while placement is open. Shell-only: one new `dash` rising edge in `EdgeReader`; no sim/net/content/tuning change (sim golden log untouched). | left/right was the only free directional pair for "choose a character," so the two things it used to do had to move: team folds cleanly into card-column parity (one fewer control), and bot management becomes a modal owned by the host (the dash button was unused in menus). One-occupant-per-card is the simplest faithful reading of "if a character is chosen, another player can't choose it" and needs no hover-vs-lock distinction | multi-player hover of the same card before lock rejected (adds a hover/lock split for no couch-play benefit); keeping bot add/remove on the host's left/right rejected (asymmetric — the host couldn't re-pick its own character); a dedicated team-switch button rejected (column parity already encodes it) |
